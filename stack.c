#include "monty.h"

/**
 * stack - set the format of the data to a stack
 * @head: pointer to pointer to the head of the stack
 * @line: current line number
 *
 * Return: nothing
 */
void stack(stack_t **head, unsigned int line)
{
	(void)head;
	(void)line;

	gvar.s_q = 1;
}
